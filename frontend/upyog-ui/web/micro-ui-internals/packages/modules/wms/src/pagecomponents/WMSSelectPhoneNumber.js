import React from "react";
import { FormStep } from "@egovernments/digit-ui-react-components";

const WMSSelectPhoneNumber = ({ config, onSelect, onSkip, t }) => {
//const SelectEmailId = ({ config, onSelect, onSkip, t }) => {
  return <div>BrSelectPhoneNumber</div>;
  //return <FormStep config={config} onSelect={onSelect} t={t}></FormStep>;
};

export default WMSSelectPhoneNumber;
